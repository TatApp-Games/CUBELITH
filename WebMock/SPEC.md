# GEODIA — Web 版 仕様書

ステータス: 初版（2026-09-04）。UE5 モバイル版の仕様書（付録 A）を Web 版の試作に写像したもの。
この文書が Web 版の仕様の正。UE 固有の記述（ISM / Niagara / Blueprint 等）は 4〜5 章で Web の実装手段に読み替えている。

## 0. 目的と位置づけ

- 本来の製品は UE5 のモバイルアプリ。その前段として、**ゲームロジックと手触りをブラウザで試作**し、生成アルゴリズム・操作感・演出の設計を検証する
- 開発の 9 割は生成 AI（Auto_Tasks の自動実装）が行う。人は要望を書き、結果を触って評価する
- 見た目（すりガラス質感）は「Web で出せる範囲で近づける」。UE 版のマテリアルを再現することが目的ではない

## 1. プロジェクト概要

| 項目 | 内容 |
|---|---|
| ジャンル | プロシージャル 3D 組み立てパズル |
| プラットフォーム | ブラウザ（PC の Chrome / Edge を基準に、スマホの Safari / Chrome でも動くこと） |
| 技術 | TypeScript + Vite + Three.js。ロジックは Three.js に依存しない純粋な TS で書く（7 章） |
| コンセプト | 「クリスタル質感」×「AI 自動生成による無限パズル」 |

## 2. コアゲームループ

1. **難易度設定**: プレイヤーが「空間サイズ N」と「分割数 M」を選ぶ
2. **自動生成**: 条件に基づき、バラバラのクリスタルピースが空中に散らばった状態で生成される
3. **パズルプレイ**: ピースをドラッグ / スワイプで移動・回転し、N × N × N の立方体に組み上げる
4. **クリア演出**: 完全な立方体が完成すると、発光と融合の演出とともに 1 つの巨大クリスタルになる
5. クリア画面から「もう一度（同じ条件で再生成）」「難易度を変える」に戻れる

## 3. ゲームシステム仕様

### 3.1 難易度・パラメータ

- **空間サイズ N**: 3〜7（整数）。既定 3
- **ピース分割数 M**: 2 〜 N³ / 4 程度の範囲で選べる（N=3 なら 2〜6、N=7 なら 2〜40 を目安。UI では 4〜6 段階のプリセットでよい）。既定は N=3 / M=4
- **乱数シード**: 生成の再現用に数値シードを持つ。UI には出さなくてよいが、URL クエリ `?seed=` で指定できると検証が楽

### 3.2 プロシージャル生成（領域拡張 / Region Growing）

1. N × N × N の 3 次元配列（各セル = ボクセル）を用意し、すべて未割り当てにする
2. ランダムな相異なる座標に M 個のシード（核）を置き、それぞれをピース id 0..M-1 に割り当てる
3. 各ピースが、自分のボクセルに隣接（6 近傍）する未割り当てボクセルを 1 つ吸収する、をラウンドロビンで繰り返す。空間全体が埋まるまで続ける
   - あるピースの周囲に空きが無ければそのラウンドはスキップ
   - すべてのピースがスキップになったのに空きが残る場合（孤立した空き）は、残りを隣接するいずれかのピースに吸収させる
4. 結果として各ピースは**連結**したボクセル集合になる（連結性はテストで保証する）
5. 各ピースのボクセル座標は「解答位置」として保持し、プレイ開始時にランダムな位置・向き（90 度単位の 24 通りのいずれか）で空中に散らす。散らす範囲は立方体の周囲（各軸 ±(N+2) 程度）で、ピース同士が重ならないようにする

### 3.3 操作システム

- **ピース選択**: クリック / タップでピースをアクティブ化。アクティブなピースは縁取りかハイライトで示す
- **移動**: アクティブなピースをドラッグ / スワイプで、**グリッド（ボクセル単位）に沿って** X / Y / Z 軸方向に移動する
  - ドラッグは画面平面上の移動をカメラの向きに応じて 2 軸に写像する。残る 1 軸（奥行き）はホイール / 2 本指の上下で動かす
  - 移動はボクセル単位で確定し、中間位置ではスナップ候補（3.5）を計算する
- **回転**: 2 本指スワイプ / ひねり、回転モードのドラッグ、または回転ギズモで、アクティブなピースを **90 度単位**で回転する。回転はピースの局所原点（最初のボクセル、または重心に最も近いボクセル）を中心に行う
- **カメラ**: ピースが選択されていないときのドラッグはカメラの旋回（OrbitControls 相当）。ホイール / ピンチでズーム
- **やり直し**: 「散らし直す（同じ配置に戻す）」ボタンを HUD に置く

### 3.4 クリア判定

- 全ピースを「グリッド座標 + 24 通りの向き」で表し、各ピースのボクセルをワールドのグリッド座標に変換する
- 変換後のボクセル集合が、**N × N × N のいずれかの位置**（原点は問わない。ピース群が作る立方体がどこにあってもよい）に重複なく・はみ出しなく収まればクリア
- 物理判定は使わない。配列のインデックス計算のみで行う。判定はピースを動かすたびに実行し、`src/core` の純粋関数として実装する（Three.js に依存しない）

### 3.5 マグネット・スナップ

- プレイヤーが手を離したとき、アクティブなピースが「正解に近い位置」（既に置かれている他のピースと隣接して整合し、かつ重ならない位置が **半マス〜1 マス以内**にある）なら、その位置へ自動で吸い付く
- 「正解に近い」の定義（初版）: 他のピースと少なくとも 1 面で接し、重なりが無く、全ピースの外接立方体が N × N × N に収まる位置。候補が複数あれば最も近いもの
- 補間: 現在位置からスナップ先へ 100〜150 ms で滑らかに移動（lerp / easing）
- 効果音: 吸い付いた瞬間にガラスが組み合う短い SE（WebAudio。音源ファイルは合成音で代用してよい）

## 4. グラフィック仕様（Web への写像）

| UE 版 | Web 版の実装手段 |
|---|---|
| Instanced Static Mesh でブロック群を描画 | `THREE.InstancedMesh`。**1 ピース = 1 InstancedMesh**（ボクセル数分のインスタンス）。ドローコールはピース数 + α に抑える |
| 動的ライトはディレクショナル 1 灯 + HDRI 環境光 | `DirectionalLight` 1 灯 + 環境マップ（`PMREMGenerator` で作った簡易 HDRI か `RoomEnvironment`）。影は無しか低解像度 1 枚 |
| すりガラス（Roughness 高めの半透明ガラス） | `MeshPhysicalMaterial`（`transmission` + `roughness` 0.4〜0.6 + `thickness`）を第一候補。重ければ半透明 `MeshStandardMaterial` + フレネル風の縁の発光に落とす |
| Fake 屈折（画面空間テクスチャの歪み） | 背景をレンダーターゲットに描き、法線で UV をずらして参照するカスタムシェーダ（`ShaderMaterial`）。**初版はスコープ外**（transmission で代用）。手触りが固まってから着手する |
| 内部発光コア（サイン波で明滅） | 各ボクセル中心に小さな発光メッシュ（InstancedMesh で 1 セット）。`emissiveIntensity` を `0.5 + 0.5 * sin(t)` で呼吸させる。ピースごとに位相をずらす |

- 目標フレームレート: PC 60 fps、スマホ 30 fps 以上（N=7 / M=40 でも）
- 解像度: `devicePixelRatio` は上限 2 でクランプする

## 5. 演出・アニメーション仕様

### 5.1 マグネット・スナップ演出

3.5 のとおり。加えて、スナップ候補がある間はピースを薄く光らせる（プレイヤーに「手を離せば吸い付く」ことを伝える）。

### 5.2 完成時のカタルシス：巨大クリスタルへの融合

クリア判定が真になった瞬間、次を **この順序で** 2〜3 秒かけて再生する。

1. **発光**: 全ボクセルの内部コアの発光強度が連動して最大まで上がり、画面全体が明るくなる（ブルームがあれば併用。無ければ材質の emissive だけでよい）
2. **融合**: ピースの境界（エッジ / 縁取り）がフェードアウトし、1 つの N × N × N の立方体メッシュに差し替わる（個別の InstancedMesh を非表示にして単一の `BoxGeometry` を表示）
3. **パーティクル**: 立方体の中心から光の粒子が放射状に広がる（`THREE.Points` + 簡単な速度・寿命更新。数百〜千粒）
4. **カメラ**: カメラが立方体の周囲を自動でゆっくり旋回する（操作は無効化。クリア画面の「もう一度」で解除）

## 6. UI

- **タイトル / 難易度選択画面**: N（3〜7）と M のプリセットを選んで「開始」。シードの表示（小さく）
- **プレイ中 HUD**: 回転モードのトグル（「回転 / 回転解除」）、「固定 / 固定解除」、「散らし直す」、「難易度へ戻る」、「ヒント」、「次の問題」、残りピース数（未確定のピース数）
- **クリア画面**: 「もう一度」「難易度を変える」
- スマホでは HUD を画面下部に寄せ、ボタンは指で押せる大きさ（44 px 以上）にする
- UI は素の HTML + CSS（フレームワーク不使用）。Three.js のキャンバスに重ねる

## 7. 技術構成

```
AutoTest/
    index.html              エントリ（Vite）
    src/
        main.ts             起動。UI とレンダラをつなぐ
        core/               純粋なゲームロジック。Three.js を import しない（テスト対象）
            grid.ts         ボクセル座標・24 通りの向き・回転行列
            generate.ts     領域拡張による分割（3.2）
            piece.ts        ピースの表現と配置変換
            solve.ts        クリア判定（3.4）とスナップ候補（3.5）
            rng.ts          シード付き乱数
        render/             Three.js。シーン・マテリアル・演出
        input/              ポインタ / タッチ入力をゲーム操作に変換
        ui/                 HTML の画面と HUD
    tests/                  Vitest。core/ の全関数に対する単体テスト
    SPEC.md / CLAUDE.md
```

- 依存: `three`, `vite`, `typescript`, `vitest`。これ以外を足すときは理由を CLAUDE.md に書く
- コマンド: `npm run dev`（開発サーバ）/ `npm test`（Vitest 一括）/ `npm run build`（型チェック込みの本番ビルド）
- `src/core` は **Three.js に依存しない**。生成・判定・スナップは全部ここに置き、テストで保証する

## 8. 実装の段階（マイルストーン）

| 段階 | 内容 | 完了の目安 |
|---|---|---|
| M0 | 雛形。Vite + TS + Three.js が動き、`npm test` と `npm run build` が通る | 画面に立方体が 1 つ回っている（**準備済み**。`src/main.ts` と `src/core/rng.ts` + テスト） |
| M1 | core: グリッド・向き・生成（領域拡張）・クリア判定・シード乱数 + テスト | N=3〜7 / M の全組み合わせで連結・網羅・重複なしをテストが保証 |
| M2 | 描画: ピースを InstancedMesh で表示、散らばった初期配置、カメラ操作 | 生成結果が見える |
| M3 | 操作: 選択・グリッド移動・90 度回転・クリア検知（演出なし） | 手でクリアできる |
| M4 | 手触り: マグネットスナップ + SE、HUD、難易度選択画面 | 一通り遊べる |
| M5 | 演出: クリア時の発光・融合・パーティクル・カメラ旋回。すりガラス質感の調整 | 見せられる |
| M6 | 最適化とスマホ対応: 30 fps 以上、タッチ操作、`?seed=` | スマホで遊べる |

要望が仕様全体を指す場合（「SPEC.md の内容で Web モックを作成して」）、分解はこの表の順に M1 から切り、1 タスクは 1 マイルストーン以下の粒度にする。後続のマイルストーンは先行の成果物（`src/core` の API など）に依存するので、タスクの詳細に前提を書く。

## 9. 受け入れ条件（verify に使えるもの）

- `npm test` が通ること（core の単体テスト）。とくに:
  - 生成: 任意の N ∈ [3,7]・有効な M・任意のシードで、全ボクセルがちょうど 1 つのピースに属し、各ピースが連結している
  - 判定: 解答配置に戻せばクリアになり、1 ピースを 1 マスずらすとクリアにならない
  - 向き: 24 通りの向きが相異なり、4 回同じ軸で回すと元に戻る
- `npm run build` が通ること（型エラー 0）
- 見た目・手触り（質感・スナップの気持ちよさ・演出）は機械判定できないので verify を省略し、人が確認する

## 10. スコープ外（Web 版では作らない）

- UE5 / C++ / Blueprint に関するもの一切
- iOS / Android のネイティブアプリ化、ストア配信
- セーブデータ、ランキング、課金
- 本物の屈折や重いポストプロセス（初版）。Fake 屈折は M5 以降の検討事項
- 音源ファイルの制作（合成音で代用）

---

## 付録 A. 原文（UE5 モバイル版の仕様書。参照用・そのまま保持）

### 1. プロジェクト概要

- ジャンル: プロシージャル3D組み立てパズル
- プラットフォーム: iOS / Android (Unreal Engine 5 モバイルレンダラー使用)
- コンセプト: 「スマホで動く極上のクリスタル質感」×「AI自動生成による無限パズル」
- 開発方針: ロジック（C++ / Blueprint）の9割を生成AIに記述させ、人間はマテリアル調整と手触りのブラッシュアップに集中する。

### 2. コアゲームループ

1. 難易度設定: プレイヤーが「空間サイズ」と「分割数」を選択。
2. 自動生成: 条件に基づき、バラバラのクリスタルピースが空中に生成される。
3. パズルプレイ: ピースをスワイプで移動・回転させ、N × N × N の立方体に組み上げる。
4. クリア演出: 完全な立方体が完成すると、極上のエフェクトと共に融合する。

### 3. ゲームシステム仕様

#### 3.1. 難易度・パラメータ設定

- 空間サイズ (N): 3 × 3 × 3 から最大 7 × 7 × 7 まで選択可能。
- ピース分割数 (M): 指定した空間サイズ内で、いくつに分割するかを選択可能。（例：3 × 3 × 3 の空間なら2〜6分割程度）

#### 3.2. プロシージャル生成（自動分割）

- アルゴリズム: 領域拡張（Region Growing）方式。
  1. N × N × N の3次元配列を用意。
  2. ランダムな座標に M 個の「シード（核）」を配置。
  3. 各シードが隣接する空きブロック（ボクセル）を吸収し、空間全体が埋まるまで拡張を繰り返す。
  4. 生成されたピース群を、プレイ開始時にランダムな座標・角度に散らばらせる。

#### 3.3. 操作システム

- ピース選択: タップで操作対象のピースをアクティブ化。
- 移動: 画面スワイプで、グリッド（ボクセル単位）に沿ってX, Y, Z軸方向に移動。
- 回転: 2本指スワイプ、または専用UIボタンで、選択中のピースを90度単位で回転（Pitch, Yaw, Roll）。

#### 3.4. クリア判定

- 全てのピースが、N × N × N のローカル座標配列内に重複なく、かつはみ出すことなくピッタリ収まった状態になったらクリアフラグを立てる。物理判定は使わず、配列のインデックス計算のみで行う。

### 4. グラフィック＆アート仕様

#### 4.1. モバイル最適化とレンダリング

- Instanced Static Mesh (ISM): 大量のブロックを描画するため、同一ピース内のブロック群はISMを利用してドローコール（描画負荷）を最小化する。
- ライティング: 動的ライトはメインのディレクショナルライト1灯のみ。環境光はHDRIを使用し、処理を軽く保つ。

#### 4.2. マテリアル仕様：「神秘的なすりガラス」

- ベース質感: 半透明のすりガラス（Roughnessを高めに設定したガラスマテリアル）。
- Fake屈折: モバイルで重い本物の屈折（Refraction）の代わりに、画面空間のテクスチャを歪ませるFake屈折シェーダーを実装し、軽量かつ美しく見せる。
- 内部発光（コア）: ブロックの中心に、うっすらと発光（エミッシブ）するコアを仕込む。呼吸するようにゆっくりと明滅（サイン波を利用）させることで神秘性を演出。

### 5. 演出・アニメーション仕様

#### 5.1. 手触りの向上：「マグネット・スナップ演出」

- 挙動: プレイヤーがピースを動かし、他のピースとピッタリ合う「正解に近い位置（半マス〜1マス以内）」に来た際、操作の手を離すと自動的に「シュッ！」と吸い付く。
- 実装: FMath::Lerp（線形補間）または Timeline ノードを使用し、現在地からスナップ先座標へ数フレームで滑らかに移動させる。
- 効果音: 吸い付いた瞬間に「カチッ」「シャリン」といったガラスが組み合う心地よいSEを鳴らす。

#### 5.2. 完成時のカタルシス：「巨大クリスタルへの融合」

- 発光演出: クリア判定がTrueになった瞬間、全ブロックの内部コアのエミッシブ強度が連動して最大まで上昇し、画面が眩しく輝く。
- 融合演出: 個別のピースの境界線（エッジ）がスッと消え、1つの巨大で完全なクリスタルキューブに変化する。
- パーティクル: キューブを中心に、光の粒子やフレアが放射状に広がるNiagara（UEのパーティクルシステム）エフェクトを再生する。
- カメラ演出: カメラが自動的にキューブの周囲をゆっくりと旋回し、完成品を美しく舐め回すように映し出す。
